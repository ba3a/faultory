# AGENTS.md

The instructions for this repository live in **[CLAUDE.md](CLAUDE.md)**. Read that file.

It is the single source of truth for module layout, architecture, naming, the sprite pipeline, the
event contract, build and test commands, shell rules, and constraints.

This file is a pointer only. It used to carry a second copy of those rules, which drifted — it still
claimed the build contained only `:core` and `:desktop` long after `:editor` was added. One
authoritative file beats two that disagree.
