package com.faultory.core.graphics

object SocketNames {
    /** On a carrier: where a held product sits. */
    const val HANDS = "hands"

    /** On a product: the spot a carrier's hands take hold of, subtracted from the carrier's socket. */
    const val GRIP = "grip"

    /** On a belt: the height a rider stands at, so climbing on does not sink into the frame. */
    const val SURFACE = "surface"
}
